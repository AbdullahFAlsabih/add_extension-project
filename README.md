# add_extension-project

This project demonstrates how to create and use a custom file extension (.abdi) in C. A struct is used to store student data, dynamic memory is allocated with malloc, and the data is written into a binary file using fwrite. The project helps understand structures, memory allocation, and binary file handling in C.
